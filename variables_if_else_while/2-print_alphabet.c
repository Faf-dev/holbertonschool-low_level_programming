#include <stdio.h>
/**
 * main - blabla
 * Return: return 0
 **/
int main(void)
{
int lettre;
int lign = '\n';
for (lettre = 'a'; lettre <= 'z'; lettre++)
	putchar(lettre);
	putchar(lign);
	return (0);
}

