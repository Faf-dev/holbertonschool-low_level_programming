#include <stdlib.h>
#include <stdio.h>

/**
 * print_alphabet - wewewe
 * main - nique ta mere
 * return: return 0
 */
void print_alphabet(void)

{
	int alpha;

	for (alpha = 'a'; alpha < 'z'; alpha++)
		putchar(alpha);
	putchar('\n');
}
