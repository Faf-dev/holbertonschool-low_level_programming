#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strcmp - check the code
 *
 * @s1 : first
 *
 * @s2 : second
 *
 * Return: Always 0.
 */

int _strcmp(char *s1, char *s2)
{
	if (s1 == s2)
	{
		return (0);
	}
	return (0);
}
