#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _strdup - copie la string donnee en parametre
 * @str : string a copier
 * Return: NULL si fail, str_cpy sinon
 */
char *_strdup(char *str)
{
	char *strcpy = malloc(
}
